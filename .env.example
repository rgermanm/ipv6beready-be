MOCK_CONTAINERS=false
CORS_ORIGINS=["http://localhost:3000"]
LAB_HOST=localhost

# Auth (JWT). Override SECRET_KEY in production.
SECRET_KEY=change-me-in-production-ipv6beready
ACCESS_TOKEN_EXPIRE_MINUTES=60
GOOGLE_CLIENT_ID=
RECAPTCHA_SECRET_KEY=

# Remote containerlab host (SSH)
CLAB_REMOTE_HOST=66.97.37.101
CLAB_REMOTE_PORT=5412
CLAB_REMOTE_USERNAME=root
CLAB_REMOTE_PASSWORD=a87sadsad00ff--asdsds1AB

# MongoDB — port 27019 is not reachable from the internet.
# From a laptop: MONGODB_SSH_TUNNEL=true (tunnels via CLAB_REMOTE_* SSH).
# On the VPS docker network: MONGODB_SSH_TUNNEL=false and host mongo-ipv6beready-prod:27017
MONGODB_URI=mongodb://admin:I6brReady--k8mQ2nP91x@66.97.37.101:27019/?authSource=admin
MONGODB_DB=ipv6beready
MONGODB_SSH_TUNNEL=true

# Per-user containerlab instance lifetime (seconds)
LAB_TTL_SECONDS=1800
